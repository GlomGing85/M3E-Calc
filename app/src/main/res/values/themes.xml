<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      The app draws every surface itself through the resolved Material 3
      colour roles, so the platform theme only needs to stay out of the way:
      no action bar, transparent system bars, and a window background that
      matches the light surface while the first frame is drawn.
    -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
